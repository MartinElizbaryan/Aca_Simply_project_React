export const getUserInfo = (state) => state.user.info

export const getUserId = (state) => state.user.info.id

export const getUserAuth = (state) => state.user.auth

export const getUserIsAdmin = (state) => state.user.info.is_admin
