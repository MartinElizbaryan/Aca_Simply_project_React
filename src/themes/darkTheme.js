import { createTheme } from "@mui/material/styles"

export default createTheme({
  palette: {
    mode: "dark",
    blurBlue: {
      main: "rgba(0,0,0,1)",
      blur: "rgba(0,0,0,0.8)",
    },
    body: "#000",
    blueButton: {
      main: "#000",
      hover: "#505050",
    },
    greenButton: {
      main: "#000",
      hover: "#505050",
    },
  },
})
